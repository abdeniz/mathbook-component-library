export const fontFamily = '"Poppins", sans-serif'
export const rootEm = 16

export const spacing = {
    default: `${rootEm}px`,
    half: `${rootEm / 2}px`,
    quarter: `${rootEm / 4}px`,
    double: `${rootEm * 2}px`,
    threeQuarters: `${rootEm * 0.75}px`
}

export const fontSizing = {
    h1: `${rootEm * 2}px`,
    h2: `${rootEm * 1.5}px`,
    h3: `${rootEm * 1.25}px`,
    paragraph: `${rootEm}px`,
}

export const colors = {
    white1: '#EEEEEE',
    white2: '#FAFAFA',
    dark1: '#232931',
    dark2: '#393E46',
    contrast: '#4ECCA3',
    error: '#FF5959',
}